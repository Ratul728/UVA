#include<bits/stdc++.h>

using namespace std;

int main()
{

    int t,a[3], m = 1;

    cin>>t;

    while(t--)
    {
        cin>>a[0]>>a[1]>>a[2];

        sort(a, a+3);

        printf("Case %d: %d\n", m,a[1]);

        m++;


    }

    return 0;

}
